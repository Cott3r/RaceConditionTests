#include <iostream>
#include "Worker.h"
#include "GradientDescent.h"

using namespace std;

int main(int argc, char* argv[])
{
//  Worker* worker = new Worker();
//  worker->doWork();

/*
  GradientDescentPosition gd_position = GradientDescentPosition();
  gd_position.addDimension(new GradientDescentDimension("X", 0.0));
  gd_position.addDimension(new GradientDescentDimension("Y", 0.0));
  gd_position.addDimension(new GradientDescentDimension("Z", 0.0));

  GradientDescent gd = GradientDescent(gd_position, 100000);

  gd.findMultiplePositions();
*/
  return 0;
}
